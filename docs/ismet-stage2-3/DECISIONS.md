# DECISIONS

## D1 — Existing clone reused, not a second fresh clone

The master prompt asks for "a fresh clone on your own computer." This session already has a fully-fetched, clean working clone of the exact same remote at `C:\Users\Administrator\Desktop\biological-minimalism`. Rather than create a redundant second clone on disk (which risks confusing which copy is authoritative), I fetched `origin` fresh, verified the working tree was clean/uncontaminated, stashed one unrelated pending change from a prior unrelated task (see STATUS.md), and branched directly from the verified `origin/codex/stage1-scientific-data-integrity` SHA. This satisfies the intent (a clean, verified starting point at the exact required base) without the risk of a second untracked directory.

## D2 — Branch tracking

`git checkout -b ismet/frontend-stage2-3-hardening origin/codex/stage1-scientific-data-integrity` set the new branch's upstream to the Stage-1 branch by default. This will be corrected to track the new branch's own remote ref only if/when it is pushed (`git push -u origin ismet/frontend-stage2-3-hardening`), so a plain `git push` never accidentally targets the Stage-1 branch.

## D3 — A2 reduced-motion precedence: OR, not override

Master prompt A2 requires "a persisted app setting OR OS `prefers-reduced-motion: reduce` (either)" to produce one effective value. Precedence rule adopted: **either source being true makes the effective preference true; there is no way to override the OS preference back to full motion from within the app, and there is no way for the OS preference to suppress a persisted opt-in.** This exactly mirrors the pre-existing CSS-layer behavior in `globals.css` (the `html.reduce-motion` class rule and the `@media (prefers-reduced-motion: reduce)` rule are two independent selectors that both apply the same `!important` override — neither can cancel the other), so the new JS-layer hook (`useReducedMotionPreference()` in `lib/runtime/reduceMotion.ts`) does not introduce a second, disagreeing model of precedence.

Found and fixed during this same milestone: Framer Motion's `animate`/`transition` props run through its own JS animation engine, not CSS `animation`/`transition` properties, so the existing CSS override never reached them — `DigitalTwinPanel.tsx`'s orbiting rings (26s/34s `repeat: Infinity`) and pulsing glow (2.4s `repeat: Infinity`) kept animating at full speed under reduced motion prior to this fix, in both the OS-only and persisted-setting cases. Fixed via a root-level `<MotionConfig reducedMotion={reduced ? "always" : "never"}>` (`components/layout/MotionConfigProvider.tsx`), driven by the same shared hook, rather than editing each animated component individually.

Also found: `OperationalPhysiologyStage.tsx` and `ConceptualTwinStage.tsx` each independently re-implemented an OS-only `matchMedia("(prefers-reduced-motion: reduce)")` check, meaning a user who enabled ONLY the persisted `/settings` toggle (not the OS setting) still got full WebGL rotation on the physiology avatar and the conceptual twin. Both now read the shared hook instead of maintaining their own copy — this is the concrete "no component may disagree" failure mode A2 was written to catch.

## D4 — Milestone B: `origin/claude/deployment-hardening` reconciliation scope

Before porting anything, computed the actual diff scope rather than assuming the whole branch needed reconciling. `git merge-base origin/claude/deployment-hardening origin/codex/stage1-scientific-data-integrity` resolved to `c99ed8f`, and `git merge-base --is-ancestor c99ed8f origin/codex/stage1-scientific-data-integrity` confirmed that commit is already fully contained in the accepted Stage 1 lineage. That means the branch's **entire unique contribution is its single top commit**, `ef747182353560d6355931310a08bcce5d3a949d` ("chore(deployment): harden jury environment reproducibility") — everything below it is already in Stage 1. This collapsed what the master prompt describes as a potentially large, overlapping-history reconciliation into a review of one self-contained 17-file commit, verified file-by-file against the current tree rather than merged or cherry-picked wholesale.

Ported as-is (self-contained, no conflict with Stage 1/2 work, each independently verified against the current repo state): `frontend/.env.local.example`, `frontend/Dockerfile` (npm ci + lockfile copy, `NEXT_PUBLIC_WS_URL` build arg), `frontend/src/lib/config.ts` (`firstConfiguredValue` trimming/empty-as-unset, `deriveWebSocketUrl` with trailing-slash handling), `docker-compose.yml` (`BIOMIN_ALLOWED_ORIGINS`, `NEXT_PUBLIC_WS_URL` build arg), `backend/.env.example` and `backend/app/core/config.py` (exact-origin CORS validator — rejects wildcard/blank/empty, trims whitespace), `backend/tests/test_cors_config.py`, `backend/tests/test_deployment_contract.py`, `backend/tests/test_jury_verifiers.py`, `scripts/verify_jury_environment.py`, `scripts/verify_jury_release_evidence.py`, `docs/JURY_DEPLOYMENT_RUNBOOK.md`, `docs/JURY_RELEASE_EVIDENCE_MANIFEST.md`, `docs/DATASET_REPLAY.md` (operator-only-setup note), `README.md` (`npm ci` correction + runbook pointer).

**Not ported**: `docs/claude-reports/CLAUDE_C1_DEPLOYMENT_HARDENING_REPORT.md` — that prior session's own self-report about doing this work; not required deliverable content for this task, and porting it verbatim would have re-introduced into the repo the exact misleading claim documented below (§13 of that report literally quotes the uncorrected wording as its own "after" state).

**Corrected, not ported verbatim (the master-prompt "Known C1" issue)**: the source commit's fix for `DataSourceControl.tsx`'s internal-env-var leak replaced `"Set BIOMIN_PPG_DALIA_PATH in the backend environment to enable replay."` with `"...Contact the demo administrator or run the presenter preflight to enable it."` — the second half of that sentence is itself false: Presenter Preflight (`components/operations/PresenterPreflight.tsx`, confirmed by reading it) is a purely read-only diagnostic surface (`derivePresenterPreflight` renders ten independent facts; nothing in it mutates backend state or the dataset). Running Preflight cannot "enable" replay. Applied instead: `"Recorded PPG-DaLiA replay is not available on this deployment. An operator must configure the dataset on the backend; use Presenter Preflight to check current readiness."` — Preflight diagnoses, it does not fix. Also corrected one instance of the same ambiguity in `docs/JURY_DEPLOYMENT_RUNBOOK.md` §25 ("points the user to the demo administrator / presenter preflight" → explicit "Presenter Preflight ... does not enable replay, change backend configuration, or repair a missing dataset itself"). Confirmed via repo-wide grep that no other instance of the misleading phrasing exists.

Verified after porting: full backend suite (`pytest`, 351/351 passed, including the 3 newly-ported deployment/CORS/jury-verifier test files), both jury verifier scripts run for real against the actual repo root (`verify_jury_environment.py`: 20 PASS / 6 WARN / 0 FAIL — WARNs are all genuinely-absent tools/env vars, never fabricated; `verify_jury_release_evidence.py`: honestly reports `F-07 status: INCOMPLETE`, 23/24 artifacts MISSING, since this checkout carries no QA screenshot tree — never claims evidence that doesn't exist), and the full frontend gate (`verify:monitoring` 933/933 + all 5 structural checks, lint, `tsc --noEmit`, `next build` 14/14, `git diff --check`) — all clean. Docker/Compose validation itself was NOT executed (Docker is not installed in this environment, confirmed via `docker --version` failing) — reported as `NOT_RUN_DOCKER_UNAVAILABLE`, not inferred as passing from static inspection.

## D5 — Milestone C: canonical bootstrap sequencing extracted as a pure function

The actual risk in "Load Canonical Jury Demo" is its race-safety/sequencing behavior (double-invocation, owner transfer, unmount mid-sequence), not its individual REST calls — those already exist and are already tested at the `api`/backend layer. Rather than writing the four-step sequencing loop directly inside the React-bound `MonitoringSessionContext.tsx` callback (where, like `resetDemoState`, it would only be reachable by rendering a component), it was extracted as `runCanonicalJuryBootstrap` in `presenterOps.ts` — a framework-free async function taking its REST runners and its "am I still current" check as plain injected parameters. This makes the actual race-safety-critical logic directly testable with real promise-ordering control (a local `deferred()` helper) in the existing Node-only `verify-monitoring-state.ts` harness, without needing a DOM or React renderer — closing exactly the kind of gap the coordinator's user explicitly flagged ("structural guards may supplement but must not be the sole evidence where runtime behavior can be tested"). The React-bound wiring in `MonitoringSessionContext.tsx` is now a thin adapter (connect the pure sequencer to `api.*` and to `sourceStateRequestCoordinator`'s ticket), mirroring exactly how `resetDemoState` already relates to `planDemoReset`.

Chose NOT to independently re-derive HR-checkpoint availability as a client-side prerequisite check, because the backend API contract does not expose that fact at all (confirmed: `derivePresenterPreflight`'s own "HR model / checkpoint readiness" item is honestly `"unknown"`, never inferred ready). Fabricating a checkpoint check the frontend cannot actually perform would violate the project's own "never infer ready from absence of an error" rule just as much as fabricating an inference result would. Documented as a known, honest gap rather than silently omitted.
